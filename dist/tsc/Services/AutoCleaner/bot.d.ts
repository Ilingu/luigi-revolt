import { FunctionJob } from "../../lib/types/types";
export declare const acDeleteImages: (chId: string, msgIds: string[]) => Promise<FunctionJob>;
declare const StartACBot: () => void;
export default StartACBot;
